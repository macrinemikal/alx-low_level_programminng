#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 *@k: the int to check.
 * Return: Absolute value of int.
 */
int _abs(int k)
{
	if (k >= 0)
	{
	return (k);
	}
	return (-k);
}
